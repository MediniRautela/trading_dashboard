# FastAPI and server
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.6

# Database
sqlalchemy[asyncio]>=2.0.25
aiosqlite>=0.19.0
asyncpg>=0.29.0  # For PostgreSQL in production

# Authentication
python-jose[cryptography]
bcrypt
passlib[bcrypt]

# Validation and settings
pydantic>=2.5.0
pydantic-settings>=2.1.0
email-validator>=2.1.0

# Rate limiting
slowapi>=0.1.9

# Data processing
pandas>=2.0.0
numpy>=1.24.0

# Existing ML dependencies (for integration)
torch>=2.2.0
yfinance>=0.2.50
pyyaml>=6.0.0

# CORS and security
python-dotenv>=1.0.0

# WebSocket support (included in uvicorn[standard])
# WebSocket support (included in uvicorn[standard])
websockets>=12.0

# HTTP Client
httpx>=0.27.0
